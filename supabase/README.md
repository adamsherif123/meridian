# Supabase

1. Create a project at https://supabase.com
2. Open **SQL Editor → New query**, paste `schema.sql`, and click **Run**.
3. Copy your **Project URL** and **service_role** key from **Settings → API** into `backend/.env`.

The backend uses the service-role key (server-side only — never sent to the browser).
